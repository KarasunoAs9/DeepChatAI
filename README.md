# 🧠 MyPsyAI

![MyPsyAI Banner](https://img.shields.io/badge/MyPsyAI-AI%20Psychologist-blue?style=for-the-badge&logo=openai)

**MyPsyAI** — это веб-приложение для психологической поддержки с использованием искусственного интеллекта. Приложение предоставляет интуитивный интерфейс для общения с AI-психологом, поддерживает векторный поиск по документам и обеспечивает безопасную аутентификацию пользователей.

## 🌟 Особенности

### 🔥 Основной функционал
- **Интеллектуальный чат** с AI-психологом на базе OpenAI GPT-4o-mini
- **Реал-тайм общение** через WebSocket соединения
- **Векторный поиск** по документам с использованием FAISS
- **Многопользовательская система** с безопасной аутентификацией
- **История чатов** с возможностью создания новых диалогов
- **Современный UI** с адаптивным дизайном

### 🛡️ Безопасность
- JWT токены для аутентификации
- Хеширование паролей с bcrypt
- CORS настройки для безопасного взаимодействия frontend-backend
- Валидация данных с использованием Pydantic

### 🚀 Технологии

#### Backend
- **FastAPI** — современный, быстрый веб-фреймворк для API
- **Tortoise ORM** — асинхронная ORM для работы с базой данных
- **PostgreSQL** — надежная реляционная база данных
- **LangChain 1.0** — фреймворк для работы с LLM
- **FAISS** — библиотека для эффективного векторного поиска
- **Poetry** — управление зависимостями Python

#### Frontend
- **React 18** — современная библиотека для создания пользовательских интерфейсов
- **TypeScript** — типизированный JavaScript для надежности
- **Tailwind CSS** — утилитарный CSS фреймворк
- **Vite** — быстрый сборщик и dev-сервер
- **Radix UI** — компоненты для создания доступного интерфейса

## 📋 Требования

- **Python** >= 3.9 (рекомендуется 3.13)
- **Node.js** >= 18
- **PostgreSQL** >= 15
- **OpenAI API ключ**
- **Rust** (для сборки некоторых зависимостей, устанавливается автоматически)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/MyPsyAI.git
cd MyPsyAI
```

### 2. Настройка переменных окружения

Создайте файл `backend/.env`:

```bash
cd backend
cat > .env << 'EOF'
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/deep-chat-ai
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256
OPENAI_API_KEY=your-openai-api-key-here
EOF
```

**Важно:** Замените `your-openai-api-key-here` на ваш реальный API ключ от OpenAI.

### 3. Настройка базы данных

Убедитесь, что PostgreSQL запущен и создайте базу данных (или используйте существующую `deep-chat-ai`):

```bash
# Проверка подключения
pg_isready -h localhost -p 5432

# Создание базы данных (если нужно)
createdb -h localhost -U postgres deep-chat-ai
```

### 4. Установка зависимостей

#### Backend

```bash
cd backend

# Установка зависимостей через Poetry
poetry install --no-root

# Или установка напрямую в виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt  # если есть
# или установка через pip напрямую
pip install fastapi uvicorn[standard] tortoise-orm asyncpg python-jose passlib bcrypt envparse langchain langchain-openai langchain-community langchain-core openai faiss-cpu python-multipart langchain-text-splitters websockets
```

**Примечание:** Если возникают проблемы с установкой `tiktoken` (требует Rust), установите Rust:
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source "$HOME/.cargo/env"
```

#### Frontend

```bash
cd frontend
npm install
```

### 5. Запуск приложения

#### Backend

```bash
cd backend
source .venv/bin/activate  # или poetry shell
python -m app
```

Сервер запустится на `http://localhost:8000`

#### Frontend

```bash
cd frontend
npm run dev
```

Frontend будет доступен на `http://localhost:5173` (или другом порту, который укажет Vite)

### 6. Доступ к приложению

После успешного запуска приложение будет доступно по адресам:

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:8000
- **API документация**: http://localhost:8000/docs
- **PostgreSQL**: localhost:5432

## 🛠️ Альтернативные способы запуска

### Запуск через Poetry

```bash
cd backend
poetry run python -m app
```

### Запуск через uvicorn напрямую

```bash
cd backend
source .venv/bin/activate
uvicorn src.main:app --reload --host 0.0.0.0 --port 8000
```

### Запуск через Docker (опционально)

```bash
# Сборка и запуск всех сервисов
make build
make up

# Или в режиме разработки
make dev
```

## 🏗️ Архитектура проекта

```
MyPsyAI/
├── backend/                    # FastAPI backend
│   ├── src/
│   │   ├── core/              # Основные конфигурации
│   │   ├── models/            # Tortoise ORM модели
│   │   ├── routers/           # API маршруты
│   │   ├── schemas/           # Pydantic схемы
│   │   ├── services/          # Бизнес-логика
│   │   └── utils/             # Утилиты (AI агент, векторный поиск)
│   ├── app/                   # Точка входа для python -m app
│   ├── .env                   # Переменные окружения (создать вручную)
│   ├── Dockerfile
│   └── pyproject.toml
├── frontend/                   # React frontend
│   ├── components/            # React компоненты
│   ├── lib/                   # Утилиты и API клиент
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml         # Production конфигурация
├── docker-compose.dev.yml     # Development конфигурация
└── Makefile                   # Команды управления
```

## 📡 API Документация

Backend предоставляет RESTful API с автоматической документацией:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные эндпоинты:

- `POST /auth/register` — Регистрация пользователя
- `POST /auth/sign_in` — Авторизация
- `GET /user/me` — Получение информации о текущем пользователе
- `GET /chat/my` — Получение списка чатов пользователя
- `POST /chat/new_chat` — Создание нового чата
- `GET /chat/{chat_id}/history` — Получение истории чата
- `WebSocket /ws/{chat_id}?token=...` — Реал-тайм чат
- `WebSocket /ws/new?token=...` — Создание нового чата через WebSocket

## 🔌 WebSocket API

Приложение использует WebSocket для реал-тайм общения:

### Подключение
```javascript
const ws = new WebSocket(`ws://localhost:8000/ws/${chatId}?token=${accessToken}`);
```

### Формат сообщений

**Отправка сообщения:**
```json
{
  "type": "user_message",
  "message": "Привет! Как дела?"
}
```

**Получение ответа AI:**
```json
{
  "type": "ai_response",
  "message": "Привет! У меня всё отлично, спасибо!",
  "message_id": 123,
  "timestamp": "2024-01-15T10:30:00Z"
}
```

## 🔧 Настройка

### Переменные окружения

Файл `backend/.env` должен содержать:

```env
# База данных
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/deep-chat-ai

# Безопасность
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256

# OpenAI API
OPENAI_API_KEY=your-openai-api-key-here
```

### Конфигурация AI

Настройки AI агента можно изменить в файле `backend/src/utils/agent.py`:

- Модель OpenAI (по умолчанию: gpt-4o-mini)
- Температура генерации (по умолчанию: 0.7)
- Промпт системы (настроен как психолог Анна Владимировна)
- Инструменты для агента

## 🚨 Troubleshooting

### Частые проблемы

1. **Ошибка подключения к базе данных**
   - Убедитесь, что PostgreSQL запущен: `pg_isready -h localhost -p 5432`
   - Проверьте правильность `DATABASE_URL` в `.env`
   - Убедитесь, что база данных существует

2. **Ошибка установки зависимостей (tiktoken)**
   - Установите Rust: `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh`
   - Установите переменную окружения: `export PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1`
   - Переустановите зависимости

3. **Ошибка с bcrypt/passlib**
   - Убедитесь, что установлена версия `bcrypt<5.0.0`: `pip install "bcrypt<5.0.0"`
   - Пароли автоматически обрезаются до 72 байт

4. **WebSocket не работает**
   - Установите `websockets`: `pip install websockets`
   - Установите uvicorn с поддержкой WebSocket: `pip install "uvicorn[standard]"`
   - Перезапустите сервер

5. **Ошибки импорта langchain**
   - Проект использует LangChain 1.0, который имеет другую структуру импортов
   - Убедитесь, что установлены все зависимости: `langchain`, `langchain-openai`, `langchain-community`, `langchain-core`, `langchain-text-splitters`

### Логи

```bash
# Логи backend (если запущен через python -m app)
# Логи выводятся в консоль

# Логи через Docker (если используется)
docker-compose logs -f backend
docker-compose logs -f frontend
```

## 🧪 Тестирование

### Backend тесты

```bash
cd backend
poetry run pytest
# или
source .venv/bin/activate
pytest
```

### Frontend тесты

```bash
cd frontend
npm run test
```

## 📦 Сборка для продакшена

### Docker образы

```bash
# Сборка всех образов
docker-compose build

# Сборка конкретного сервиса
docker-compose build backend
docker-compose build frontend
```

### Оптимизация

1. **Backend**: Приложение использует uvicorn для продакшена
2. **Frontend**: Vite собирает оптимизированный bundle
3. **PostgreSQL**: Использует индексы для быстрых запросов

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Стиль кода

- **Backend**: Используйте black для форматирования, flake8 для линтинга
- **Frontend**: Используйте ESLint и Prettier
- **Commit messages**: Используйте conventional commits

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).

## 🙏 Благодарности

- [OpenAI](https://openai.com/) за предоставление API
- [FastAPI](https://fastapi.tiangolo.com/) за отличный фреймворк
- [React](https://reactjs.org/) за мощную библиотеку UI
- [LangChain](https://langchain.com/) за инструменты работы с LLM

---

**MyPsyAI** — делаем психологическую поддержку доступной с помощью искусственного интеллекта! 🚀
